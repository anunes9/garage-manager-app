{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "RAILS_ENV=production bin/rails db:prepare && RAILS_ENV=production bin/rails server",
    "restartPolicyType": "ON_FAILURE"
  }
}
